@inherits ComponentBase

<Bar Breakpoint="Breakpoint.Desktop"
     Background="Background.Light"
     ThemeContrast="ThemeContrast.Light"
     Mode="BarMode.VerticalInline"
     CollapseMode="BarCollapseMode.Hide">
    <BarMenu>
        <BarStart>
            <BarItem>
                <BarDropdown Visible="true">
                    <BarDropdownToggle>
                        Repros
                    </BarDropdownToggle>

                    <BarDropdownMenu>
                        <BarDropdownItem To="/selectlist" Title="SelectList">
                            SelectList
                        </BarDropdownItem>

                        <BarDropdownItem To="/datagrid" Title="DataGrid">
                            DataGrid
                        </BarDropdownItem>
                    </BarDropdownMenu>
                </BarDropdown>
            </BarItem>
        </BarStart>
    </BarMenu>
</Bar>

@code {

}
